# Fincard 经济系统说明

> 本文档由当前代码与配置整理，数值口径对应 `app/economy_builder.gd` 与 `data/config/economy.json`。
> 结算方式：`EconomyModel.tick` 每回合先快照上回合指标 → 用快照**同时**计算所有增量（互不污染）→ 应用增量 → 计算派生指标并写入 `state.indicators`。

---

## 1. 指标一览

| id | 显示名 | 性质 | 有方程演化 | bounds |
| --- | --- | --- | --- | --- |
| `gdp` | 国内生产总值 | 水平存量（货币） | 是（乘法增长） | 无 |
| `debt` | 政府债务 | 水平存量（货币） | 是 | 无 |
| `inflation` | 通货膨胀 | 百分点 | 是（弱锚回归） | `[-20, 40]` |
| `unemployment` | 失业率 | 百分点 | 是（弱锚回归） | `[0.5, 60]` |
| `confidence` | 市场信心 | 0–100 点 | 是（弱锚回归） | `[0, 100]` |
| `interest_rate` | 利率 | 百分点 | **否**（纯外生，仅效果/修正） | `[0, 25]` |
| `tax_rate` | 税率 | 百分点 | **否**（纯外生，仅效果/修正） | `[0, 70]` |
| `growth` | 经济增长率 | 派生（% / 回合） | 派生 = GDP 增长率 | `[-8, 8]` |
| `debt_ratio` | 债务率 | 派生 | 派生 = 债务 / GDP（夹取后） | `[0, 5]` |

**初始值**

| 指标 | 示例国 | 阿卡迪亚 |
| --- | --- | --- |
| gdp | 100 | 128 |
| inflation | 3 | 2 |
| unemployment | 6 | 5 |
| debt | 60 | 30 |
| confidence | 50 | 70 |
| interest_rate | 3 | 5 |
| tax_rate | 20 | 25 |

---

## 2. 中性锚（基准值）

| 系数 | 值 | 作用 |
| --- | --- | --- |
| `natural_unemployment` | 6 | 失业率的回归目标 |
| `neutral_rate` | 3 | 利率的"中性"基准（缺口为 0 时无拖累/刺激） |
| `neutral_tax` | 20 | 税率的中性基准 |
| `debt_threshold` | 0.9 | 债务率超过此值才触发"挤出"惩罚 |
| `confidence_neutral` | 50 | 信心的回归目标 |
| `inflation_target` | 2 | 通胀的回归目标 |

---

## 3. 系数总表

| 系数 | 默认值 | 含义 |
| --- | --- | --- |
| `gdp_auto` | 0.3 | 潜在增长（%/回合） |
| `gdp_conf` | 0.15 | 信心每偏离中性 1 点对增长的影响（/100） |
| `gdp_rate_gap` | 0.02 | 利率每高于中性 1 点对增长的拖累 |
| `gdp_tax_gap` | 0.03 | 税率每高于中性 1 点对增长的拖累 |
| `gdp_crowd` | 1.0 | 债务率每超阈值 1.0 对增长的拖累 |
| `infl_anchor` | 0.1 | 通胀向目标回归速度（**弱**，可按国覆盖） |
| `infl_phillips` | 0.15 | 失业每低于自然率 1 点推高通胀 |
| `infl_demand` | 0.1 | 增长率每 1% 推高通胀 |
| `infl_rate` | 0.1 | 利率每高于中性 1 点压低通胀 |
| `u_okun` | 0.25 | 增长率每 1% 降低失业 |
| `u_revert` | 0.05 | 失业向自然率回归速度（**弱**，可按国覆盖） |
| `debt_rev` | 0.05 | 税率 100% 时的收入（占 GDP） |
| `debt_int_pass` | 0.25 | 利息负担 = 债务率 × 利率 |
| `conf_growth` | 0.5 | 增长对信心的提振 |
| `conf_infl` | 0.2 | 通胀对信心的打压 |
| `conf_debt` | 4.0 | 债务率超阈值部分对信心的打压 |
| `conf_anchor` | 0.1 | 信心向 50 回归速度（**弱**，可按国覆盖） |

> 已移除的旧系数：`gdp_stim`、`infl_stim`、`conf_stim`、`debt_spend`（预算规模的自动经济通道，见第 5 节）。

**按国覆盖（`economy_overrides`）**：剧本可在 `data/scenarios/*.json` 里覆盖任意系数，未列出的沿用默认。

| 系数 | 通用默认 | 示例国（市场/自由） | 阿卡迪亚（干预主义） |
| --- | --- | --- | --- |
| `infl_anchor` | 0.1 | 0.15 | 0.05 |
| `u_revert` | 0.05 | 0.07 | 0.03 |
| `conf_anchor` | 0.1 | 0.12 | 0.05 |

（占位数值，可自由调整；体现"市场化经济体自然修复更快、干预主义更慢"。）

---

## 4. 方程逐条（代入默认数值）

### 4.1 增长率 `growth`（派生，每回合 %）

```
growth = gdp_auto
       + gdp_conf      × (信心 − 50) / 100
       − gdp_rate_gap  × (利率 − 3)
       − gdp_tax_gap   × (税率 − 20)
       − gdp_crowd     × max(0, 债务率 − 0.9)

代入：
growth = 0.3
       + 0.15 × (信心 − 50)/100
       − 0.02 × (利率 − 3)
       − 0.03 × (税率 − 20)
       − 1.0  × max(0, 债务率 − 0.9)
```

### 4.2 GDP

```
ΔGDP = GDP × growth / 100        （乘法，非金币式线性累加）
```

### 4.3 通胀（百分点 / 回合）

```
Δ通胀 = 0.1  × (2 − 通胀)          ← 向目标弱回归
      + 0.15 × (6 − 失业)          ← 菲利普斯曲线
      + 0.1  × growth              ← 需求拉动
      − 0.1  × (利率 − 3)          ← 货币政策沟道
```

### 4.4 失业（百分点 / 回合）

```
Δ失业 = 0.05 × (6 − 失业) − 0.25 × growth
```
（弱回归自然率 + 奥肯定律）

### 4.5 债务（存量）

```
Δ债务 = GDP × [ − debt_rev × 税率/100 + debt_int_pass × 债务率 × 利率/100 ]

代入：Δ债务 = GDP × [ − 0.05 × 税率/100 + 0.25 × 债务率 × 利率/100 ]
```
支出项已移除：**预算本身不产生赤字**。债务只由"税收收入（减债）""利息负担（增债）"以及卡牌/事件**显式效果**决定。

### 4.6 信心（0–100 点 / 回合）

```
Δ信心 = 0.5 × growth
      − 0.2 × 通胀
      − 4.0 × max(0, 债务率 − 0.9)
      + 0.1 × (50 − 信心)
```

### 4.7 派生指标

```
growth     = 见 4.1
debt_ratio = clamp(债务 / GDP, 0, 5)     （与 UI/胜负条件同口径，见第 8 节第 4 点）
```

---

## 5. 预算与经济的解耦（重要）

- 选择预算档位只确定两件事：**财政预算池**（下一年可花多少）与**政治行动点补充**，外加档位声明的一次性 `effects`。
- **预算规模不再是经济输入**：原 `gdp_stim/infl_stim/conf_stim/debt_spend` 四个系数已删除；`budget_scale`、预算档位的 `scale` 字段、`rules.base_budget_scale` 均已移除。
- 经济变化改为**完全由内容自身的效果驱动**：卡牌/事件/内阁/援助在被使用/触发时通过 `effects` 改变指标。即"预算只有真正花出去打出卡牌才起作用，且作用由该卡的效果决定"。

---

## 6. 互相关系（联动网络）

### 6.1 有向关系逐条

| 源 → 目标 | 方向 | 通道 | 条件 |
| --- | --- | --- | --- |
| 利率 → growth | 负 | `gdp_rate_gap` | 恒 |
| 利率 → 通胀 | 负 | `infl_rate` | 恒 |
| 利率 → 债务 | 正 | 利息负担 `debt_int_pass` | 债务率 > 0 时 |
| 税率 → growth | 负 | `gdp_tax_gap` | 恒 |
| 税率 → 债务 | 负 | 收入 `debt_rev` | 恒 |
| 债务率 → growth | 负 | `gdp_crowd` | 仅 > 0.9 后 |
| 债务率 → 信心 | 负 | `conf_debt` | 仅 > 0.9 后 |
| 债务率 → 债务 | 正 | 利息负担 | 恒（自强化） |
| 债务 → 债务率 | 正 | 分子 | 恒 |
| GDP → 债务率 | 负 | 分母 | 恒 |
| GDP → 债务增量 | 正 | 缩放因子 | 恒 |
| 信心 → growth | 正 | `gdp_conf` | 恒 |
| growth → 通胀 | 正 | `infl_demand` | 恒 |
| growth → 失业 | 负 | `u_okun` | 恒 |
| 失业 → 通胀 | 负 | `infl_phillips` | 恒 |
| 通胀 → 信心 | 负 | `conf_infl` | 恒 |
| 通胀 → 通胀 | 负 | `infl_anchor` | 向目标 2（弱） |
| 失业 → 失业 | 负 | `u_revert` | 向自然率 6（弱） |
| 信心 → 信心 | 负 | `conf_anchor` | 向中性 50（弱） |
| 效果 / 修正器 → 任意指标 | 任意 | ADD / MUL / OVERRIDE | 卡牌/事件/内阁等 |

### 6.2 反馈环

| 环 | 路径 | 性质 |
| --- | --- | --- |
| 信心—增长 | 信心 ↑ → growth ↑ → 信心 ↑ | **正反馈（放大）** |
| 债务螺旋 | 债务率 ↑ → 利息 ↑ → 债务 ↑ → 债务率 ↑ | **正反馈（自强化）** |
| 通胀—信心 | 通胀 ↑ → 信心 ↓ → growth ↓ | 负反馈 |
| 需求—通胀 | growth ↑ → 通胀 ↑ | 负反馈（经信心） |
| 菲利普斯 | 失业 ↓ → 通胀 ↑ | 负反馈 |
| 奥肯 | growth ↑ → 失业 ↓ | 负反馈 |
| 挤出 | 债务率 > 0.9 → growth ↓ / 信心 ↓ | 负反馈 |
| 利率双刃 | 利率 ↑ → 通胀 ↓ + growth ↓ + 债务 ↑ | 权衡 |
| 税率权衡 | 税率 ↑ → 债务收入 ↑ + growth ↓ | 权衡 |
| 全局弱锚回归 | 通胀→2、失业→6、信心→50（**较弱**） | 负反馈（自稳，但慢） |

---

## 7. 修正器与效果语义

- **修正器求值**：指标最终值 = 基础值依次叠加所有修正器
  `ADD: 值 += value`、`MUL: 值 *= value`、`OVERRIDE: 值 = value`
  `duration` 正数 = 剩余回合数，`-1` = 永久（`EconomyModel.value` 返回最终值，`state.indicators[id]` 为基础值）。
- **修正器有作用域 `scope`**：
  - `indicator`（默认）：作用于指标最终值（`EconomyModel.value`）。
  - `coefficient`：作用于经济系数（`EconomyModel.coefficient`），用于在运行时快速改变数值体系。
- **系数修正**：`{ "kind": "apply_modifier", "params": { "scope": "coefficient", "target": "gdp_auto", "op": "add", "value": 0.5, "duration": -1, "source": "国家精神·重商主义" } }`。系数取值分四层叠加：
  1. 全局默认（`economy.json` 的 `coefficients`）；
  2. 剧本覆盖（`economy_overrides`）；
  3. 国家精神（剧本 `innate_modifiers`，通常 `duration: -1`）；
  4. 运行时内容（内阁 `modifiers`、改革卡/事件/援助的 `effects`）。
  后一层叠加在前一层之上（ADD/MUL/OVERRIDE 按 `state.modifiers` 顺序）。`mul` + 限时 `duration` 是最容易制造爆炸式增长的组合。
- **`adjust_indicator`**：直接改基础值；`percent: true` 时 `delta = amount/100 × GDP`，**仅**对 `gdp`/`debt` 生效，对其它目标会按绝对值处理并告警。
- **`branch`**：if/else（`then`/`else`），可嵌套。
- **效果可带 `condition`**：不满足则整条效果跳过。
- **`bounds` 可被剧本覆盖**：剧本 `bounds` 用 `[min,max]` 覆盖某指标边界，用 `null` 移除边界；`start_run` 时先 `reset_bounds()` 再套用覆盖，跨剧本复位。
- `bounds` 在 tick 应用增量时与 `adjust_indicator` 调值处统一夹取。

---

## 8. 值得注意的点

1. **预算与增长已彻底解耦**：不再存在"选高预算→自动高增长"的通道。好处是符合"预算只是预算"的设计；代价是**增长几乎完全依赖卡牌效果与信心**（`gdp_auto` 仅 0.3%/回合打底）。若某些卡效果偏弱，经济会长期低迷——数值平衡的重心从系数移到了**卡牌效果**上。

2. **弱锚是双刃剑**：把 `infl_anchor` 从 0.3 降到 0.1、`u_revert` 从 0.08 降到 0.05 后，通胀/失业的自然修复明显变慢、稳态漂移更大（中性条件下失业会低于自然率、通胀更易走高）。这符合"市场自然回归应当弱"的意图，但也意味着**更容易被单个事件推离轨道**，需要靠利率/税率政策与卡牌主动纠偏。由于是可覆盖系数，建议按剧本实测微调。

3. **支持率并入信心后的正反馈放大**（上一版改动延续）：所有"民心"效果现在都注入 `confidence`，而信心直接驱动增长，`事件 → 信心 → 增长 → 信心` 的放大回路较强。这是当前最需要关注的稳定性来源。

4. **债务率双口径已统一**（本次修正）：`_debt_ratio()` 现在按 `bounds` 夹取后再返回，方程、派生指标、UI、胜负条件使用**同一个**债务率，消除了此前"动力学用原始值、判定用夹取值"的分叉。

5. **债务不再有自动的支出累积通道**：预算花出去**不会**自动增加债务，只有卡牌/事件显式 `adjust_indicator debt` 才加。设计上简洁，但需确保"借债类"卡牌都显式写了债务效果，否则会出现"疯狂花钱却不欠债"。

6. **`confidence` 的硬拐点仍在**：挤出只在债务率超过 `0.9` 后生效，且 `conf_debt = 4.0` 很大，跨越阈值时是非线性突变。

7. **利率 / 税率为纯外生，无自动稳定器**：两者没有方程，不会随通胀或债务自动反应（无泰勒规则）。弱锚之下，缺乏自动稳定器的后果更明显。

8. **`percent` 现已校验目标**：非 `gdp`/`debt` 使用 `percent` 会按绝对值处理并输出告警（此前会静默产生荒谬数值）。

9. **中性条件下的打底数值（仅供参考，未计政策与卡牌）**：
   - 潜在增长 ≈ `0.3%`/回合 → 年化约 **+3.7%**
   - 通胀、失业的稳态随锚强度与剧本差异较大，不再给出单一收敛值。

10. **硬边界代替软限幅**：`bounds` 是硬夹，撞到边界时增量为 0（数值"贴墙"），可能出现非平滑行为；`CHANGELOG §17` 已列为待办。
