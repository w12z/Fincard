# Fincard 经济系统说明

## 1. 游戏里有哪些数字

| 数字 | 中文名 | 它是什么 | 是否自行变化 | 上下限 |
| --- | --- | --- | --- | --- |
| `gdp` | 国内生产总值 | 国家经济总量 | 会（按百分比增长） | 不限 |
| `debt` | 政府债务 | 政府欠债总额 | 会 | 不限 |
| `inflation` | 通货膨胀 | 物价上涨速度 | 会（较慢） | -20 ~ 40 |
| `unemployment` | 失业率 | 失业人口占比 | 会（较慢） | 0.5 ~ 60 |
| `confidence` | 市场信心 | 市场对经济的信心 | 会（较慢） | 0 ~ 100 |
| `interest_rate` | 利率 | 借贷成本 | 不会（只能由内容改变） | 0 ~ 25 |
| `tax_rate` | 税率 | 税收比例 | 不会（只能由内容改变） | 0 ~ 70 |
| `growth` | 经济增长率 | GDP 每回合变化的百分比 | 自动计算 | -8 ~ 8 |
| `debt_ratio` | 债务率 | 债务 ÷ GDP | 自动计算 | 0 ~ 5 |

`利率` 与 `税率` 不会自行变化，只能由卡牌等内容改变；其余数字每回合自动演化。

**开局数字**

| 数字 | 示例国 | 阿卡迪亚 |
| --- | --- | --- |
| GDP | 100 | 128 |
| 通胀 | 3 | 2 |
| 失业率 | 6 | 5 |
| 债务 | 60 | 30 |
| 信心 | 50 | 70 |
| 利率 | 3 | 5 |
| 税率 | 20 | 25 |

## 2. 每回合的结算顺序

一回合分四步：`回合开始 → 抽牌 → 出牌 → 回合结束`。一年 12 个回合。

回合结束时统一结算经济：

1. 记录当前所有数字（快照）；
2. 用快照**同时**计算每个数字本回合的变化量（互不影响）；
3. 应用变化量（达到上下限则截断）；
4. 计算两个自动派生数字：经济增长率与债务率。

限时修正在回合结束时减少 1 回合，减到 0 失效；永久修正（`duration` 为 `-1`）不减少。

## 3. 数值之间的影响关系

| 从 | 到 | 方向 | 说明 |
| --- | --- | --- | --- |
| 利率 | 增长、通胀 | 负 | 利率越高，增长与通胀越低 |
| 利率 | 债务 | 正 | 利率越高，利息支出越多 |
| 税率 | 增长 | 负 | 税率越高，增长越低 |
| 税率 | 债务 | 负 | 税率越高，税收越多，债务越少 |
| 债务率 | 增长、信心 | 负 | 仅在债务率超过 0.9 后生效 |
| 债务率 | 债务 | 正 | 债务越高，利息越多，债务继续增加 |
| 信心 | 增长 | 正 | 信心越高，增长越快 |
| 增长 | 通胀 | 正 | 经济过热推高通胀 |
| 增长 | 失业 | 负 | 增长越高，失业越低 |
| 失业 | 通胀 | 负 | 失业越低，通胀越容易被推高 |
| 通胀 | 信心 | 负 | 通胀越高，信心越低 |
| 通胀、失业、信心 | 自身 | 回归目标 | 分别向 2、6、50 缓慢回归 |
| 卡牌 / 事件效果 | 任意数字或系数 | 任意 | 增加、相乘或直接设定 |

主要反馈关系：

- **信心与增长互相促进**：信心上升推动增长，增长又提升信心，容易持续放大。
- **债务自我强化**：债务越高，利息支出越大，债务进一步增加。
- **信心是核心变量**：它是唯一能自行推动增长的数字，且多数"民心"类效果最终都作用于信心，因此整个经济对信心的变化很敏感。

## 4. 系数（规则参数）与修改方式

系数是公式中的参数，例如"信心每高于 50 一点，增长率增加 0.15/100"中的 `0.15`。默认值写在 `data/config/economy.json`。

系数来源分四层，依次叠加：

1. **全局默认**（`economy.json`）；
2. **剧本覆盖**（剧本中的 `economy_overrides`）；
3. **国家精神**（剧本中的 `innate_modifiers`，通常永久）；
4. **运行中的修改**（内阁、卡牌、事件、援助的效果）。

修改方法：给效果加 `"scope": "coefficient"`，`target` 填系数名，`op` 取 `add`/`mul`/`override`，`duration` 填回合数或 `-1`（永久）。

```json
{ "kind": "apply_modifier", "params": { "scope": "coefficient", "target": "gdp_auto", "op": "add", "value": 0.2, "duration": -1, "source": "国家精神·自由市场" } }
{ "kind": "apply_modifier", "params": { "scope": "coefficient", "target": "infl_anchor", "op": "mul", "value": 0.5, "duration": 4 } }
```

- `add`：在原值上增加，适合渐进调整。
- `mul`：乘以倍数，增长最快，适合限时爆发；多个 `mul` 会连乘，容易过快。
- `override`：直接设为固定值，最可控，适合一次性改变规则。

**全部系数**

| 系数 | 默认值 | 含义 |
| --- | --- | --- |
| `natural_unemployment` | 6 | 失业率的回归目标 |
| `neutral_rate` | 3 | 利率的中性水平 |
| `neutral_tax` | 20 | 税率的中性水平 |
| `debt_threshold` | 0.9 | 债务率超过此值后开始拖累经济 |
| `confidence_neutral` | 50 | 信心的回归目标 |
| `inflation_target` | 2 | 通胀的回归目标 |
| `gdp_auto` | 0.3 | 基础增长率（%/回合） |
| `gdp_conf` | 0.15 | 信心每高于 50 一点，增长增加多少 |
| `gdp_rate_gap` | 0.02 | 利率每高于 3 一点，增长减少多少 |
| `gdp_tax_gap` | 0.03 | 税率每高于 20 一点，增长减少多少 |
| `gdp_crowd` | 1.0 | 债务率超出 0.9 的部分，每 1.0 拖累增长多少 |
| `infl_anchor` | 0.1 | 通胀向目标回归的速度（越小，通胀越难压低） |
| `infl_phillips` | 0.15 | 失业每低于 6 一点，通胀被推高多少 |
| `infl_demand` | 0.1 | 增长率每 1%，推高通胀多少 |
| `infl_rate` | 0.1 | 利率每高于 3 一点，通胀被压低多少 |
| `u_okun` | 0.25 | 增长率每 1%，降低失业多少 |
| `u_revert` | 0.05 | 失业向目标回归的速度（越小，危机后越难恢复） |
| `debt_rev` | 0.05 | 税率 100% 时的税收（占 GDP 比例） |
| `debt_int_pass` | 0.25 | 利息负担 = 债务率 × 利率 × 此系数 |
| `conf_growth` | 0.5 | 增长对信心的提升 |
| `conf_infl` | 0.2 | 通胀对信心的压制 |
| `conf_debt` | 4.0 | 债务率超出 0.9 的部分对信心的压制 |
| `conf_anchor` | 0.1 | 信心向 50 回归的速度 |

## 5. 具体计算公式

**经济增长率 `growth`（每回合 %）**

```
growth = 0.3
       + 0.15 × (信心 − 50) / 100
       − 0.02 × (利率 − 3)
       − 0.03 × (税率 − 20)
       − 1.0  × max(0, 债务率 − 0.9)
```

**GDP**

```
GDP = 上回合 GDP × (1 + growth / 100)
```

**通胀（每回合变化的百分点）**

```
Δ通胀 = 0.1  × (2 − 通胀)      ← 向目标 2 缓慢回归
      + 0.15 × (6 − 失业)      ← 失业低于自然率时推高物价
      + 0.1  × 增长率          ← 需求拉动
      − 0.1  × (利率 − 3)      ← 加息压低通胀
```

**失业率（每回合变化的百分点）**

```
Δ失业 = 0.05 × (6 − 失业) − 0.25 × 增长率
```

**债务（每回合变化的金额）**

```
Δ债务 = GDP × [ − 0.05 × 税率/100 + 0.25 × 债务率 × 利率/100 ]
                  税收收入                利息负担
```

注意：**花费预算不会自动变成债务**。债务只由税收、利息，以及卡牌/事件中明确写出的加债效果决定。

**信心（每回合变化的点数）**

```
Δ信心 = 0.5  × 增长率
      − 0.2  × 通胀
      − 4.0  × max(0, 债务率 − 0.9)
      + 0.1  × (50 − 信心)
```

**自动派生**

```
经济增长率 growth = 上述第 1 条
债务率 debt_ratio = clamp(债务 / GDP, 0, 5)
```

## 6. 财政预算与政治行动点

| 资源 | 获取方式 |
| --- | --- |
| **财政预算** `budget` | 每财年按预算档位**重新给满**（非累加）；出牌消耗 |
| **政治行动点** `political_capital` | 每财年给一大笔（累加）+ 每回合给小一笔；出牌消耗 |

- 每回合小笔行动点 = `rules.political_capital_per_turn`（1）+ 相关修正。
- 每张卡有两种成本：预算与政治行动点。
- 预算档位不影响经济，只提供预算、行动点和档位自身的一次性效果；经济变化全部来自卡牌与事件的效果。

**现有预算档位**

| 档位 | 预算 | 行动点 | 选定后立即发生 |
| --- | --- | --- | --- |
| example_budget | 10 | 5 | 信心 +1 |
| 平衡预算案 | 8 | 8 | 信心 +3、失业 +1 |
| 稳健扩张案 | 14 | 5 | 信心 +1 |
| 赤字支出案 | 20 | 3 | 债务 +1.5%、通胀 +1、信心 −2 |
| 紧急救济案 | 26 | 2 | 信心 +2、债务 +3%、信心 −3 |

## 7. 卡牌与事件如何改变数值

修改数值的东西称为修正器，分两种作用范围：

- **改数字**（默认）：`target` 填数字名。
- **改系数**：加 `"scope": "coefficient"`，`target` 填系数名。

三种算子：`add`（加）、`mul`（乘）、`override`（设为）。`duration` 正数为限时回合数，`-1` 为永久。

效果类型：

| 类型 | 作用 |
| --- | --- |
| `adjust_indicator` | 直接加减某个数字；加 `"percent": true` 时按"占 GDP 的百分比"计算（仅适用于 GDP 与债务，用于其它目标时按普通数值处理并报出警告） |
| `apply_modifier` | 添加一个修正（改数字或改系数） |
| `remove_modifier` | 移除指定修正 |
| `draw_cards` / `gain_budget` / `gain_political_capital` | 抽牌 / 增加预算 / 增加行动点 |
| `branch` | 条件分支（如果…就…否则…），可嵌套 |

任何效果都可以附带 `condition`（条件），条件不满足时不生效。

## 8. 取值范围与数值膨胀

- 每个数字有上下限，达到边界后不再变化。
- 剧本可用 `bounds` 覆盖取值范围：用 `[min, max]` 修改，用 `null` 移除某个上限。每年开局先恢复默认，再套用剧本的设置。
- 若要允许数值大幅增长，移除对应的上限即可。

## 9. 两个国家的参数差异

| 项目 | 示例国（偏自由市场） | 阿卡迪亚（偏政府干预） |
| --- | --- | --- |
| 回归速度 | 较快（通胀 0.15 / 失业 0.07 / 信心 0.12） | 较慢（通胀 0.05 / 失业 0.03 / 信心 0.05） |
| 国家精神 | 「自由市场」：基础增长率 +0.2（永久） | 无 |
| 事件 | 随机 1 个，无定时事件 | 随机 12 个 + 定时 3 个（第 24/26/28 回合的崩盘） |
| 目标 | GDP ≥ 110 且通胀 ≤ 5（5 年内） | 失业 ≤ 8 且 GDP ≥ 135（6 年内，第 28 回合后才判定胜利） |

即：自由市场国家恢复较快、较稳定；干预主义国家恢复较慢，危机后长期处于低迷状态。

## 10. 当前内容规模

| 类型 | 数量（含样板） |
| --- | --- |
| 卡牌 | 18 |
| 事件 | 17 |
| 内阁 | 7 |
| 援助 | 4 |
| 预算档位 | 5 |
| 剧本 | 2 |
| 组 | 12 |

由于预算不再影响经济，**卡牌与事件的效果是经济变化的唯一来源**，数值平衡主要在内容上，而不是通过调整系数。

## 11. 数值量级与均衡分析

- **基础增长率较低**：不受其它因素影响时约 0.3%/回合，一年约 +3.7%。
- **失业率会被增长带动**：默认参数下，均衡失业率 ≈ `6 − 5 × 增长率`。增长 0.3% 时约为 4.5；若增长为 −4.5%，约为 28.5。因此"自然失业率 6"只是回归目标，并不固定。
- **债务自我扩大的临界条件**：当 `债务率 × 利率 > 0.2 × 税率` 时，利息超过税收，债务开始自行增加。默认（利率 3、税率 20）约当债务率超过 1.33；利率 5 时约超过 0.8。
- **事件的影响远大于自然变化**：一次崩盘事件可减少 GDP 4.5%、并使失业率上升 4 点，相当于抵消十几到二十回合的自然增长。因此一局的成败主要取决于玩家如何使用卡牌应对。

## 12. 注意点

1. 允许数值膨胀，上下限是唯一限制；需要放开时，在剧本中移除对应的 `bounds`。
2. 回归速度较慢，通胀、失业、信心不易自行回到目标，危机后主要依靠玩家操作恢复。
3. 信心是关键变量：它是唯一能推动增长的数字，也是多数"民心"效果的落点。
4. 没有自动稳定机制，利率与税率不会自行调整。
5. 慎用 `mul` 修改系数：多个乘法会连乘，容易导致数值迅速失控。结构性、永久性修改建议用 `add` 或 `override`，限时爆发再用 `mul`。
6. 债务在无内容干预时通常会自行下降（税收大于利息），只有卡牌或事件明确增加债务时才会进入上升循环。
7. 通胀公式使用上一回合的增长率（滞后一回合），该滞后是确定性的。
